# openclaw-config

Repositorio para versionar el workspace/config de OpenClaw y poder migrarlo fácilmente a otra máquina.

## Notes
- No commitear secretos (tokens/passwords/keys). Ver `.gitignore`.
- Para migrar: clona este repo en el directorio de workspace de OpenClaw.
